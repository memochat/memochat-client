import { ComponentMeta, StoryObj } from '@storybook/react';

import ChatListEmpty from './ChatListEmpty';

export default {
  component: ChatListEmpty,
} as ComponentMeta<typeof ChatListEmpty>;

export const Default: StoryObj<typeof ChatListEmpty> = {};
